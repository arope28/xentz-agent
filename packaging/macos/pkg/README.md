pkg packaging skeleton for xentz-agent.

Expected behavior:
- Install binary to /usr/local/bin/xentz-agent
- Install LaunchAgent (user mode) or LaunchDaemon (system mode)
- Run postinstall to enroll using supplied token/server
